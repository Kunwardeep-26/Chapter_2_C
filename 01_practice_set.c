#include <stdio.h>

int main(){

    // int a; b=a; ---> invalid
    // int a;
    // int b = a; ---> valid
    int v = 3^3; -- valid
    char dt = '21'; -- invalid need one character
    return 0;
}