#include <stdio.h>

int main(){
    // making two variables

    int x = 2;
    int y = 3;
    // showing the calculation

    printf("The value of 3x - 8y is %d\n", 3*x - 8*y);
    printf("The value of 8y / 3y is %d\n", 8*y / 3*x);
     return 0;
}