#include <stdio.h>

int main(){
     //making the number variables    

    int x = 2;
    int y = 3;
    int z = 3;
    int k = 1;
    // showing the calculation

    printf("The value of 3x / y -z +k is %d\n", 3 * x / y - z + k);
    
    return 0;
}